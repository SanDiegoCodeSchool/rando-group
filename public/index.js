import { get } from "https";

function gatherInput(name, skill) {
    var newName =  document.getElementById('Name');
    var newSkill = document.getElementById('Skill Level');
    console.log(newName, newSkill);
}

